# Current Milestone: S75 (Completed)

**Goal:** AI 接入 TeachingScript — AI 润色教学文案 + 模板开关，规则系统为质量关口
**依据:** 产品/UX Agent 第 5 轮确认的 S75 范围（AI 职责 / 硬边界 / 文案约束）

**Completed:**
- [x] OllamaProvider 新增公共 `generate(prompt)`（复用 callOllama，失败返回 null）
- [x] `lib/teaching-script-ai.ts`：
      buildPrompt（面向中学生的润色 prompt：只允许改 phase_cards[].hint /
      event_pulses[].text_override / force_callouts 开关；禁改 id/公式映射；≤30/≤20 字；不剧透答案）
      extractAiHintsJson（普通 JSON / markdown 代码块 / 垃圾输入）
      mergeAiHints（纯函数：AI 覆盖三组，formula_strips 永远来自规则版——教学红线）
      polishTeachingScriptWithAI（offline/超时/解析失败/校验丢弃 → 无感知回退规则版）
- [x] 硬边界落实：AI 输出必须过 validateTeachingScript，校验丢弃任一 AI 提供组 → 整体回退；
      Ollama 离线无感知降级（不新增 UI 提示）
- [x] 渲染层"空数组 = 禁用模板"语义：phase_cards/formula_strips/force_callouts/event_pulses
      四组均支持（AI 模板开关生效；缺省=规则推导不变）
- [x] 管线接入：problem.store.submit 在解析成功后、注册插件前调用 polish
- [x] 清理 FYI：删除生成器未使用的 clip()
- [x] 测试：merge/extract/polish（有效润色/非法回退/模板关闭/垃圾回退/离线回退）+
      空数组禁用语义，共 +14 用例
- [x] 验证：tsc ×3 零错、vitest 19 文件 156 用例全过、生产构建成功

**待产品/UX Agent 验收（S75 验收要点）：**
1. AI 在线：文案更口语、更引导式，且不剧透答案
2. AI 离线：无感知回退规则版，教学图层完整可用
3. 人为非法 AI 输出：校验器拦截，回退规则版

**Deferred:**
- Ollama e2e 对比运行（需本机 Ollama 在线实测 AI 润色效果）
- PDF 输入 | P2 第二步

**Next: S76** — 依验收反馈修复；或 Ollama 在线实测 AI 润色 + 解析 e2e 对比
