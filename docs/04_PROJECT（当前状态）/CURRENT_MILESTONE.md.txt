# Current Milestone: S76 (Completed)

**Goal:** Ollama 在线实测（AI 润色）+ PDF 题目输入
**依据:** 产品/UX Agent 第 6 轮确认的 S76 范围 + PDF UX 要点

**Completed:**
- [x] PDF 题目输入（首页「📄 导入 PDF 题目」次级按钮，与 OCR 并排，不新增视觉层级）
      - lib/pdf.ts：pdfjs-dist 文本提取（多页拼接、无逐页预览）
      - 流程：PDF → 提取文字 → 填入输入框（可编辑）→ 用户手动点 CTA（不自动提交）
      - 失败态：一行红字 + 引导文字输入，不阻塞首页主流程；空 PDF 单独提示
      - i18n +6 键（home.pdf*，zh/en 对齐）
      - 新增依赖：pdfjs-dist（worker 由 Vite 正确打包为独立 asset）
- [x] Ollama 在线实测脚本：scripts/eval-ai-polish.ts（力学/电学 3 题，规则版 vs AI 版对比输出 JSON）
      - 本机 Ollama 未运行（localhost:11434 超时）→ 脚本正确跳过并提示，在线实测待 Ollama 启动后执行
- [x] vite-env.d.ts（?url 模块类型）；修复 artifact 清理误删手写 .d.ts 的问题（已恢复）
- [x] 验证：tsc ×3 零错、vitest 19 文件 156 用例全过、生产构建成功（pdf.worker 1.26MB asset 正常产出）

**阻塞/待办：**
- Ollama 在线实测被本机 Ollama 未运行阻塞：启动 `ollama serve` 并 pull 模型（如 llama3.2）后，
  运行 `npx vite-node -c vitest.config.ts scripts/eval-ai-polish.ts`，结果回报产品/UX Agent 调 prompt

**Deferred:**
- PDF 逐页预览（本期不做）；Ollama 解析 e2e 对比（run-e2e.ts 已支持，同样待 Ollama 在线）

**Next: S77** — Ollama 在线实测结果处理（调 prompt / 验收）+ 后续 P1 深化或 P2 第二步验证
