# Current Milestone: S72 (Completed)

**Goal:** P1 剩余模板（ForceCallout / EventPulse）+ P2 信息架构第一步 + 实验切换器收进实验库抽屉
**依据:** TEACHING_LAYER_SPEC.md v1.1 §6-7 + 产品/UX Agent 第 2 轮验收批复

**Completed:**
- [x] ForceCallout 受力标注：screen-space 跟随（drei Html 投影）、#EF4444 语义色、
      排序取前 3 + "+N" 收敛、拖动 Timeline 确定性重建（state=f(t)）
- [x] EventPulse 事件标记：橙色扩散脉冲（仅播放态越过事件帧触发，拖动/跳转不重复触发）、
      解释文字 2s 时间窗显隐（f(t)）、Timeline 事件节点标记 + 窗口内高亮、
      每 phase ≤2（collision 必标 + 首个 other）缺省规则
- [x] overlay_hints 扩展：force_callouts / event_pulses（仅追加、向后兼容）
- [x] P2 第一步：顶栏三模式切换器移除；实验切换器收进画布"实验库"抽屉；
      学习场景默认 = 画布 + Timeline + TeacherPanel；左缘 4 图标竖排入口
      （参数/图表/步骤/图谱）开关抽屉；面板组合持久化（LAYOUT_VERSION 3）；
      PhaseCard 归位 top-16px；Dashboard 保留于 MenuBar
- [x] i18n：zh/en 各 +7 键（rail.*、menu.view.force_callout/event_pulse）
- [x] 验证：shared/desktop tsc 0 错、vitest 17 文件 131 用例全过、生产构建成功

**待产品/UX Agent 验收（TEACHING_LAYER_SPEC §7.3）：**
1. ForceCallout：力出现/消失同步、镜头旋转跟随稳定、同屏 >3 收敛
2. EventPulse：仅播放触发、拖动不重复、文字时间窗正确
3. 默认开合生效、组合可记忆、三模式切换器已移除且原功能全部可达

**Deferred:**
- S73：FormulaStrip 变量符号 #00D4FF 着色（非阻塞建议）；P2 第二步（结构合并，待验证标准达标）

**Next: S73** — 依验收反馈修复 + FormulaDisplay token 着色；或教学动画生成器（P1）
