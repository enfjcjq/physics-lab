# CASE_STUDY — 从一句话需求到可交付产品的完整决策链

> 本文档以"面向部署工程师（FDE）复盘"的视角，记录 Physics Lab 从需求理解、方案取舍、红线制定到多轮交付的全过程。所有事件均可在 `docs/` 与交流记录中溯源。

---

## 1. 背景与角色

**产品**：AI 物理教学动画引擎——学生输入任意物理题，生成有教学节奏的可交互动画。

**我的角色**：项目负责人（一人），通过双 Agent 协作模式工作——我承担产品判断/体验设计/验收测试，工程 Agent 承担实现。全程以文档驱动：42 轮设计-工程问答、20+ Sprint 计划与交付、3 份正式设计决策记录（DD-001~003）。

**为什么这个模式值得讲**：它本质上是 FDE 的日常——你不懂所有代码细节，但你必须能定义验收标准、判断交付质量、在含糊需求中守住产品底线。

## 2. 需求判断：教学视频为什么无效

初始需求是"给学生看物理教学视频"。**我的关键判断：视频是反的**——学生不是缺少"看"的材料，而是在"为什么"的环节掉队。痛点反推：

- 看得见结果，看不见**过程**（速度如何累积、力如何作用）
- 节奏是录制者的，不是学习者的（太快、不可回溯、不可分步）
- 错题是**自己的**，视频讲的是**别人的**题

→ 结论：需要"任意题目 → 定制动画"，而不是内容库。这个判断决定了整个架构方向（解析器 + 生成器，而非 CMS）。

## 3. 核心方案：Hybrid 2D+3D

第一版全部用 3D 渲染。用户实测反馈"视觉笨拙、学生会晕"。**根因分析**：平面物理问题被强行 3D 化——透视、光照、3D 文字反而成为理解负担。

**方案重做**（DD-003）：
- 平面问题 → 纯 2D 矢量动态图解（"会运动的物理示意图"：扁平、几何、少即是多）
- 空间问题 → 3D 建立空间直觉 → 锁定视角 → 在该视角投影上做 2D 图解
- 2D/3D 同一 PhysicsScene 数据源、五色语义统一（红=力/青=速度/琥珀=能量/橙=事件）

**取舍**：不为平面题保留 3D 视图（砍一半维护面）；先修播放节奏再建 2D 系统（学生立刻受益 vs 长期重构）；渲染器就绪门控（未完成的场景默认留在 3D，不为占位符牺牲可用体验）。

## 4. 教学红线：宁可诚实失败，不可错误动画

这是本项目最重要的产品原则，来自一个朴素判断：**教错比不教更糟**。三条红线案例（全部真实发生、全部拦截修复）：

1. **"写一首关于春天的诗" → 弹簧振子动画**：云端 AI 正确拒绝了非物理请求，但降级链把拒绝当失败，规则解析器把英文 "spring"（春天）误读为"弹簧"硬凑了动画。修复：区分"技术失败可降级"与"内容拒绝必须呈现"。
2. **电路题 → 自由落体动画**：云端返回的场景 topic 不在映射表里，系统静默回退到默认场景。修复：未知 topic 必须诚实报错，禁止回退。
3. **斜面 2D 速度矢量方向错 108°**：滑块向左下滑，箭头指向右下。修复前被验收拦截——**2D 图解教错物理是红线违背**，无论动画多流畅。

红线工程化的代价：多个 Sprint 的解析器/降级链/渲染器返工。但它是产品灵魂——13 题实测矩阵中最终 0 错误动画。

## 5. 交付节奏：Sprint 模式与验收纪律

- **Sprint 切分**：S71-S88 按功能域垂直切分（快修包 → 2D 地基 → 原语库 → 逐场景迁移），每个 Sprint 独立可验收
- **验收纪律**（本项目最多的一类工作）：代码级 diff 走查 + 运行时实测（Electron CDP 自动化）双轨。曾拦截：路由硬编码踩在时序竞态上"碰巧工作"（教训：行为验证必须配合实现审查）、HMR 半更新造成的假回归（教训：验收必须全新进程）
- **排期干预**：两次把工程拉回优先级——红线修复优先于新功能（P0-C/D 插队 S88）、渲染器就绪门控先于场景扩张

## 6. 质量工程

- **门禁**：185 用例 + 双包 tsc + 生产构建，每个 Sprint 全绿才验收
- **确定性红线**：`state = f(t)` 纯函数渲染，Timeline 任意拖动逐元素一致（验收方法：跳走再跳回，比对 SVG 完全一致）
- **E2E 方法论**：13 题真实题目矩阵（10 正题 + 3 边界：非物理/缺参数/超纲），每题记录云端调用/解析来源/加载插件/教学脚本四维
- **物理正确性抽验**：矢量比例尺（1N=6px）、能量守恒（PE+KE 恒定）、公式代入值逐点核对

## 7. 事故复盘（两例）

**事故一：API key 反复丢失**。用户三次反馈"配置好的 key 消失"。排查链：userData 存放 %TEMP%（系统清理可整目录删除）→ 迁移至 %APPDATA% 后仍丢失 → 最终定位强杀进程（taskkill /F）导致 localStorage 未刷盘 + 双实例 leveldb 竞争。**根治方案**：设置迁移到主进程 settings.json（Electron 惯例），彻底绕开渲染进程 localStorage 的刷盘问题。教训：配置存储的位置决策也是产品决策。

**事故二：同一功能三周内三次"通过"又三次返工**（2D 播放冻结 → 路由竞态 → 矢量方向）。每次都是验收环节拦截，代价是进度慢，收益是**没有一个错误动画流向用户**。FDE 视角的结论：验收不是流程负担，是产品最后的防线。

## 8. 结果

- 端到端闭环：题目输入 → AI 解析（云端+规则双链路）→ 4 种 2D 场景渲染器（自由落体/欧姆/平抛/斜面）+ 3D 全场景 → 教学节奏回放
- 13 题实测：红线 0 违背、诚实失败正确率 100%（该拒绝的都拒绝了）、云端成功场景参数 100% 保留
- 42 轮双 Agent 协作文档、3 份设计决策记录、完整 Sprint 交付史——**每个决策可溯源**

---

*本文档随项目演进持续更新。详细过程见 `docs/05_HISTORY/DESIGN_DECISIONS.md` 与 `docs/04_PROJECT/DESIGN_STATUS.md`。*
