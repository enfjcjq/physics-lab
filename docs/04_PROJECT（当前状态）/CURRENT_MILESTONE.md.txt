# Current Milestone: S85 (Completed)

**Goal:** 云 AI 接入（DD-002 确认）——CloudProvider + 设置页 + 三态 AI 状态 + 诚实失败联动
**依据:** 产品/UX Agent 第 16 轮（S84 缺陷包验收后）；战略：先做本地需联网，完全离线版延后

**Completed:**
- [x] CloudProvider（packages/ai-parser）：OpenAI 兼容（baseUrl/apiKey/model 可配，复用 AIProvider 抽象，
      架构不变）；unsupported → 诚实失败"不能可靠生成动画"；HTTP 失败/超时 → 明确降级提示
- [x] 设置页 SettingsDialog（MenuBar 帮助 → 设置）：API key / baseUrl / model 配置（localStorage）、
      Provider 偏好（自动=云端优先切本地 / 云端优先 / 本地优先）、云端在线状态指示
- [x] 首页 AI 状态三态：云端在线（绿）/ 本地在线（青）/ 离线降级（红），沿用小圆点形态
- [x] Provider 选择逻辑：auto → cloud(online) > ollama > rule；cloud/local 偏好强制
- [x] Ollama 保留为可选降级档；完全离线版延后（大后期）
- [x] 测试：CloudProvider（可用性/有效解析/unsupported 诚实失败/HTTP 401 降级）4 用例
- [x] 验证：tsc ×3 零错、vitest 26 文件 179 用例全过、生产构建成功

**Next: S86** — 动画质感攻坚（产品/UX 产出清单中：缓动/相机过渡/phase 节奏/3D 减负）
