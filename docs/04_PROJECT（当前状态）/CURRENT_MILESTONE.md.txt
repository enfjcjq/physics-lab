# Current Milestone: S80 (Completed)

**Goal:** P2 第二步走查未达标修复 —— 四功能入口可发现性 + MenuBar 菜单完整可读
**依据:** 产品/UX Agent 第 12 轮走查结果（①核心路径✅ ②③未达标）+ 修复需求定稿

**Completed:**
- [x] **P0 修复 1 · IconRail 可发现性**：
      - 图标加常驻文字标签（参数/图表/步骤/图谱），宽度 48→64px，图标在上 10px 文字在下
      - 一次性呼吸高亮：首次进入场景页前 2 次显示（localStorage 记录，之后不再显示）
      - 补上 S77 遗漏的 incrementIcon 打点（此前 iconClicks 未接入）
- [x] **P0 修复 2 · MenuBar 菜单项截断**：
      - 根因：右侧下拉菜单（Language/Theme/Help）从 left-0 展开超出窗口右缘被裁
      - 修复：最右侧 3 个下拉改为 right-0 对齐 + max-w/min(20rem,90vw) + max-h 80vh + overflow-y-auto
      - 窗口最小宽度 900px 评估为合理（顶栏菜单在 900px 内完整容纳；下拉不再被窗口裁切）
- [x] **P2 修复 3 · 导出入口移深**：MenuBar 帮助 → 新增「开发者」子菜单 → 导出使用数据
- [x] i18n +1 键（menu.help.developer），两端对齐 1007
- [x] 验证：tsc ×3 零错、vitest 23 文件 167 用例全过、生产构建成功

**待复走查：** 项目负责人按同一路径复走（重点：四功能入口可发现 + 菜单完整可读）→ 产品/UX Agent 重评三项指标 → 达标才启动 P2 第二步结构合并

**Next: S80 复走查** — 达标后进入 P2 第二步（模式结构合并）
