# Design Status — 产品体验与视觉设计

**更新日期：** 2026-08-28
**负责角色：** 产品体验与视觉设计 Agent
**当前设计阶段：** S71-S86 全部验收通过（17 轮问答闭环）。**2026-08-28 重大方向：动画系统重做**——S86 质感体验验证未通过（项目负责人亲测：相机不灵活/节奏难掌控/动画太快学生会晕/视觉笨拙；根因=单一 3D 渲染路径承载所有教学表达），项目负责人拍板 Hybrid 2D+3D 动画重做方向。设计规格已定稿：`02_PRODUCT（产品设计）/HYBRID_2D3D_ANIMATION_SPEC.md` v1.0（2026-08-28 项目负责人确认 Q1-Q4 全部采纳推荐：平面题纯 2D / 默认 0.8x / 音效默认关 / S87 快修包先行）。**S87 快修包（节奏 R1-R5 + 相机 C1-C3/C5）已移交工程实现 Agent（第 18 轮开启，留言 27）**；S88+ 2D 动画系统待工程评估后切分；S85 云 AI 实测待 API key。

> ⚠️ 注（2026-08-28）：本文件与 DESIGN_DECISIONS.md 疑似被回滚至较早快照（头部曾停在 S83、缺少 S84-S86 记录），已按最新事实更新头部并在下方补记；请项目负责人确认是否为有意回滚。

---

## 当前产品状态（设计视角）

- 工程版本：V4.6 / S69 完成（18 个实验、87 测试全过、0 TS 错误）
- 体验现状：功能完整的"交互式物理实验平台"，但与 Physics AI Engine 定位存在三个错位（入口错位 / 动画缺教学叙事 / 信息密度偏高）
- 设计文档：`docs/02_PRODUCT（产品设计）/UX_AUDIT_AI_ENGINE.md`（v2.0 已定稿）

## 已定稿决策（2026-08-03 项目负责人确认）

| 决策 | 定稿内容 |
|------|----------|
| **P0 首页** | 题目输入页成为启动首页；Physics Lab 实验库降级为二级入口，全部能力保留 |
| **P1 教学图层** | 模板化 Teaching Layer：建立教学解释模板系统（阶段卡 / 受力标注 / 公式推导条 / 事件标记）；AI 负责选择+组合模板并填参数，不直接生成教学视觉；低性能降级为面板联动 |
| **P2 信息架构** | 两步走：取消平级三模式，学习模式为默认核心入口，实验/解析为高级能力按需展开；第一步先调默认面板组合，验证后做结构合并 |
| **P3 颜色系统** | 五色语义系统确认（红=力 / 青=速度 / 琥珀=能量 / 橙=关键事件 / slate=中性结构）；规则对用户隐藏，系统自动一致表达，零用户学习成本 |

## 下一步建议（移交工程实现 Agent）

按已确认顺序排期（2026-08-04 与工程实现 Agent 协调：UI 轨道从 S71 起，工程轨道 S70=解析质量已完成，避免编号冲突）：
1. **S71**：P0 题目输入首页 + P1 第一批模板（PhaseCard、FormulaStrip 先行）——规格已交付：`docs/02_PRODUCT（产品设计）/TEACHING_LAYER_SPEC.md`
2. **S72**：P1 剩余模板（ForceCallout、EventPulse）+ P2 第一步（默认面板组合）
3. P2 第二步（结构合并）待第一步验证后排期；P3 色表随各提案顺带落地

## 边界与约束

- 未修改 01_FOUNDATION 任何文件
- 未改变基础架构：PhysicsScene 数据驱动、Timeline 驱动、插件系统均保持不变
- 所有改动集中于渲染层与信息架构；如需新增字段仅追加可选字段并向后兼容
- 工程实施时验证门禁保持全绿（typecheck + desktop tsc + vitest）

## 待办（设计侧）

- [x] P1 模板视觉细节规格（尺寸/动效/排版）——已交付 `TEACHING_LAYER_SPEC.md` 第 2 节（2026-08-04）
- [x] P0 首屏文案与空状态设计——已交付 `TEACHING_LAYER_SPEC.md` 第 1 节（2026-08-04）
- [x] S72 模板规格（ForceCallout / EventPulse）——已交付 `TEACHING_LAYER_SPEC.md` 第 6 节（2026-08-04）
- [x] P2 第一步规格 + 第二步验证标准——已交付 `TEACHING_LAYER_SPEC.md` 第 7 节（2026-08-04）
- [x] S71 设计走查验收——✅ 通过（2026-08-04，代码级走查；4 项实施偏差全部批准，纪要见 TEACHING_LAYER_SPEC §8）
- [x] S72 设计走查验收——✅ 通过（2026-08-04，§7.3 三项全过；3 项非阻塞观察移交 S73，纪要见 TEACHING_LAYER_SPEC §9）
- [x] S74 规格：TeachingScript Generator——已交付 `TEACHING_SCRIPT_GENERATOR_SPEC.md`（2026-08-04）
- [x] S73 设计走查验收——✅ 通过（2026-08-04，代码级核查：calloutOffset 按力类型判定、变量符号 #00D4FF、语义色 CSS 变量双主题 + IconRail 橙色激活态）
- [x] S74 设计走查验收——✅ 通过（2026-08-05，§7 四项全过；R3 偏差追认，纪要见 TEACHING_SCRIPT_GENERATOR_SPEC §8）
- [x] S75 设计走查验收——✅ 通过（2026-08-05，代码级核查：formula_strips 红线隔离、全路径无感知回退、非法输出整体拦截；AI 在线实测项转 S76）
- [x] S76 设计走查验收——✅ PDF 输入通过（2026-08-05，四项 UX 要点全过）；Ollama 实测阻塞（本机服务未运行，已上报项目负责人），转 S77
- [ ] S77 设计走查验收（实测结果评估 + P2 第二步打点支持）
- [ ] P2 第二步新用户走查（待 S77 打点就绪 + 项目负责人配合模拟新用户）

## 阻塞项（需项目负责人操作）

- ~~Ollama 未运行~~（已解决，2026-08-05 服务已启动）
- ~~模型 403 未授权~~（已解决，2026-08-05 项目负责人已安装 llama3.2 本地模型）
- 当前无用户侧阻塞项。AI 润色未生效的真根因已定位为工程问题（见下方），修复方案已移交工程 Agent。

## 新发现问题（2026-08-05 实测暴露，均已闭环）

1. ~~AI 润色全量回退~~（已修复，S77）：根因=模型回显参考 JSON + 提取器贪婪匹配；工程按留言 9 建议修复（prompt 分区重构 + 真实 id few-shot + 平衡括号提取）。我方独立复跑验证 3/3 aiApplied=true，**S75 实测项正式闭环**。
2. ~~中文题目 hint 为英文~~（已修复，S77）：方案 a 治本落地（解析器按题目语言生成教学文本），复跑数据确认规则版 hint 已中文化。
3. S78 待办：3 条 prompt 微调（重力场景保留受力标注 / 避免同义反复 / 只引用真实实体），详见交流文件留言 10。

## 里程碑（2026-08-04）

**P0-P3 全部定稿决策已完整落地**（S71+S72+S73）：题目输入首页、Teaching Layer 四模板、信息架构第一步、五色语义系统。产品形态从"实验模拟器"正式转为"题目驱动的教学动画产品"。

## 协作状态（2026-08-04）

- 与工程实现 Agent 的交流通道已建立（`D:\AIProjects\disicussion\`），轮次口径：以"问答对"计轮，状态备忘不计轮。
- 第 1 轮（已闭环）：排期协调 5 问 ↔ 规格交付答复。
- 第 2 轮（已闭环）：S71 完成报告 ↔ 验收通过。
- 第 3 轮（已闭环）：S72 完成报告 ↔ 验收通过 + S73 范围确认。
- 第 4 轮（已闭环）：S73 完成报告 ↔ 验收通过 + S74 开工确认（强调边界：纯函数确定性/快照测试、不改场景文件、三级兜底、仅追加）。
- 第 5 轮（已闭环，2026-08-05）：S74 完成报告 ↔ 验收通过 + S75 范围确认（AI 接入 TeachingScript，含文案风格约束与三条硬边界）。
- 第 6 轮（已闭环，2026-08-05）：S75 完成报告 ↔ 验收通过 + S76 范围确认（Ollama 在线实测 + PDF 输入，UX 要点已交付）。
- 第 7 轮（已闭环，2026-08-05）：S76 完成报告 ↔ PDF 输入验收通过 + S77 范围确认（实测结果处理 + P2 第二步打点支持）。
- 第 8 轮（已闭环，2026-08-05）：S77 完成报告 ↔ 验收通过（我方独立复跑实测验证 3/3 aiApplied=true，S75 实测项闭环；中文本地化与打点符合预期）+ S78 范围确认（3 条 prompt 微调 + P2 走查组织 + Ollama 解析 e2e 对比）。
- 第 9 轮（已闭环，2026-08-05）：S78 完成报告 ↔ 验收通过 + 实体幻觉定夺（**不可接受**，物理准确性是教学红线；方案：prompt 强制引用 entities[].name 原文，必要时加确定性文本替换后处理）+ S79 范围确认。
- 第 10 轮（已闭环，2026-08-05）：S79 完成报告 ↔ 验收通过（独立复跑：欧姆题"电子流"正确、真球场景无误伤、重力标注保留）+ S80 范围确认（走查结果处理；走查前不安排新功能开发）。
- 第 11 轮（已闭环，2026-08-05，状态轮）：工程顺手落地可选微修复（cleanTrailingPunctuation，去除 AI 文案结尾标点残缺；167 用例全绿），双方均无问题。
- 第 12 轮（开启，2026-08-05）：P2 第二步走查结果——**三项指标两项不达标（四图标零发现 + MenuBar 菜单项截断），结构合并不启动**。S80 = 可发现性修复（IconRail 常驻文字标签 + MenuBar 截断修复 + 导出入口深置），修复后项目负责人复走查。

## P2 第二步走查结果（2026-08-05，首次）

| 指标 | 结果 | 证据 |
|------|------|------|
| 核心路径 ≤3 次点击 | ✅ 通过 | 输入题→动画路径顺畅 |
| 四图标无零发现 | ❌ | iconClicks 全 0；用户主动找但找不到（纯图标+悬停文字不可发现） |
| 无阻塞反馈 | ❌ | 四功能入口找不到；MenuBar 菜单项文字被裁切无法辨认；导出入口误点才发现 |

**结论：P2 第二步（结构合并）不启动**，S80 修复后复走查（规格 §7.2 规则）。

**S80 修复验收（2026-08-05，第 12 轮闭环）**：✅ 通过。IconRail 64px+常驻文字标签+呼吸高亮；MenuBar 右下拉 right-0 对齐修复截断；导出入口移至帮助→开发者子菜单。注：工程坦白 S77 打点未接入（iconClicks=0 数据失效），但用户定性反馈"找了但没找到"独立成立，修复方向不变。**当前等待项目负责人复走查。**

**复走查中新发现（第 13 轮开启）**：Submenu 固定向右展开（left-full），在右对齐的「帮助」菜单下越界不可见——「导出使用数据」无法点击（P0，S80 循环内修复；临时方案：DevTools Console 取 localStorage 数据）。

**复走查结果（2026-08-08）：✅ 三项指标全部达标**（四图标 clicks 7-10 次全部被发现并多次使用；主功能无阻塞；核心路径顺畅）。**P2 第二步正式放行**，S81 = Submenu 方向修复 + 模式概念彻底退出 UI（MenuBar/i18n/面板条件分支清理，功能全保留，打点保留至确认走查）。

**S81 验收（2026-08-12，第 13 轮闭环）**：✅ 通过。Submenu 方向感知修复；模式概念退出 UI（layout 层仅存主题模式 useTheme，教学/实验/分析模式痕迹全清，WelcomeScreen 单一「开始使用」）；功能全保留、打点保留。**当前等待项目负责人做 P2 收口确认走查**（重点：UI 无模式痕迹、四功能可发现、菜单与子菜单完整可读），通过后 P2 正式收口，P0-P3 四项决策全部闭环。

**收口走查中新发现（2026-08-13，第 14 轮开启）**：HTML 实验报告导出 4 缺陷（Markdown 未转换 P0 / i18n 键名泄漏 P0 / 参数状态张冠李戴 P1 / 空知识点区块 P1），证据为用户实际导出的电动机报告。S82 = 导出质量修复（仅导出层，不改场景与教学逻辑），要求 3 类实验各导一份自验。

**第 14 轮补充（2026-08-13）**：Submenu 仍不可见的追加根因——一级下拉面板的 `overflow-y-auto` 裁剪了 absolute 定位的子菜单（方向修复代码到位但被裁剪）。修复要求：portal/fixed 渲染。临时取数：DevTools 读 localStorage。

**收口走查重大发现（2026-08-13，第 15 轮开启，P2 收口暂停）**：项目负责人截图实证——全部面板打开时扎堆中央、互相叠加、动画完全被遮盖。根因：缺布局规则（画布浮层 8+ 个互不感知、docked 面板无数量预算、教学信息三处重复出口）。S83 = 布局规则（画布 ≥40% 可视保障 / 面板同时最多 2 个 FIFO / 浮层去重编排 / 图表抽屉 ≤35% 高）+ 死菜单清理（Documentation、Version 等）+ 公式正规排版（上下标/分式/正体函数/希腊字母，与 S82 联动）。打点数据本身达标（四图标均被多次使用），但体验阻塞出现，S83 完成后重做收口走查。

**S82+S83 验收（2026-08-20，第 14、15 轮闭环）**：✅ 双双通过。S82：Markdown→HTML、i18n 键名解析、参数按场景驱动、空区块不渲染、Submenu portal 修复；S83 抽查：DOCKED_LIMIT=2+FIFO+toast、中央浮卡默认关闭、死菜单清除且六假开关接真状态、beautifyFormula 四出口接线。

**收口重走查（2026-08-20，第 16 轮开启）**：**P2 结构目标达成**（截图实证布局不再扎堆、FIFO 生效、无模式痕迹、四图标 clicks 2-5 次）。但暴露 6 项缺陷（S84 缺陷包：欢迎页 \u269B 乱码 P0 / 题目→动画正确性红线 P0 / 导出使用数据点击无效 P1 / 实验列表不能滚动 P1 / 公式代入态排版 P1 / 英文 hint 残留 P1 / 欢迎页文案遗留 P2）。

**Hammerath 风格研究 + 决策确认（2026-08-20，同日）**：研究文档 `HAMMERATH_STYLE_STUDY.md`（A/B/C 三类建议）。项目负责人全部批准：A1-A4（漂浮符号等待动画/编号步骤进度/品牌副标/CTA 精简）并入 S84；B1 双轨视觉并入 S86；B2 积分制方向确认（MVP 不做 UI，先成本测算）；B3 真实班级试用记入规划；**DD-002 云 AI 转向正式确认，S85 启动**（CloudProvider/API key 配置/AI 状态三态/诚实失败守卫）。排期：S84 → S85 → S86（动画质感攻坚）。

**S84+S85 验收（2026-08-21，第 16 轮闭环）**：✅ 双双通过（抽查：诚实失败守卫双 Provider 落实、CloudProvider、tagline/CTA i18n 键正确；门禁 179 用例全绿）。**S86 动画质感清单已交付工程**（M1 动效 token / M2 相机平滑 / M3 节奏呼吸 / M4 3D 视觉减负 / M5 图表减负 / M6 留白呼吸感，含 prefers-reduced-motion 降级约束），工程开工中。

**S86 验收（2026-08-21，第 17 轮闭环）**：✅ 通过（抽查：动效 token、相机 easeInOutCubic 阻尼插值落实；M6 部分面板留白未全命中已列入走查重点）。**当前等待项目负责人动画质感体验验证**（连续播放自由落体+电动机，感受相机/节奏/视觉重量/留白）。下一步主线候选：S85 云 AI 真实题目实测（需可用 API key）。

**Agent 交接（2026-08-23）**：前任设计 Agent 上下文耗尽，完整交接提示词已生成于 `D:\AIProjects\disicussion\设计Agent交接提示词.md`。新 Agent 接班后从"当前待办"继续，轮次从第 18 轮开始。

**新任设计 Agent 接班完成（2026-08-28）**：新任 Agent 已按交接文档完成全部必读文档阅读（00→05 全序 + 双方交流文件 1-17 轮 + AGENT_HANDOFF.md），接班确认已写入交流文件留言 26（状态备忘，不计轮）与《工程实现.txt》读取确认。

**S86 质感体验验证结果（2026-08-28，项目负责人亲测）**：**未通过**——①相机不够灵活；②节奏不好掌控；③动画演示太快，学生操作会晕头转向；④视觉效果笨拙。根因判断：问题不在质感参数，在渲染策略本身（平面问题被强行 3D 化）。修复设计不再出微调补丁，已整体并入 `HYBRID_2D3D_ANIMATION_SPEC.md`（节奏 R1-R5 / 相机 C1-C5），按 Q4 排期推进。

**动画重做方向（2026-08-28，项目负责人指示，已整理为 DD-003）**：简单二维物理过程→纯 2D 矢量动态图解；复杂/空间过程→3D 自由观察（阶段 A，拖动旋转相机）→ 锁定视角（阶段 B）→ 该视角投影平面上 2D 动画教学演示；2D 视图是重新生成的矢量图解而非隐藏 3D 模型；视觉风格=极简扁平矢量科学可视化（会运动的物理示意图）；轻量事件音效+全局开关。

**S87 代码审查（2026-08-28，设计侧全量审查，应项目负责人"体验全面崩溃"反馈启动）**：
- **结论：当前 S87 代码在所有路径下无法复现崩溃**。测试矩阵：vitest 179/179 全绿、0 lint 错误、S87 全量 diff 静态审查（状态机/tick/相机逻辑正确）；运行时实测（浏览器 + Electron CDP 双环境）：内置自由落体播放/重播/逐帧/拖动旋转/相机双模式/重置视角、题目输入→生成→自动播放（讲解节奏生效 t:0→0.70→0.85→完播）、store 直接调用 replay() 正常——**全部通过**。
- **用户崩溃体验的最可能根因（假设）**：①用户的 dev server 为 S87 提交前的旧进程，HMR 半更新导致运行态损坏（zustand 模块级状态对 HMR 敏感）——重启即恢复；②resume 机制恢复到动画结尾（currentTime=1.548/1.6），打开即静态画面被感知为"画面不会动"；③对 S88 2D 动画的预期差（2D 系统尚未开工，S87 范围仅节奏+相机）；④云 AI 静默降级（见 P0-1）被感知为"AI 功能是坏的"。
- **审查确认的真实问题（已移交工程，留言 28）**：
  - **P0-1 云状态指示诚实性**：`CloudProvider.isAvailable()` 只调 `GET /models` 验证 key，不验证模型名→模型名填错（`deepseek`，正确应为 `deepseek-chat`）时首页仍绿灯"云端 AI 已就绪"，但实际生成必失败并静默降级规则解析——状态指示说谎，违反诚实原则。建议：状态检查校验模型列表或发一次极小真实调用；设置页模型名给默认值/占位提示。
  - **P1-2 R2 未生效**：实测初始 timeScale 仍为 1（规格定稿默认 0.8x）。
  - **P1-3 C5 推荐教学视角构图差**：自由落体默认视角过高过远、球体过小、画面大面积死黑（Electron 实测截图佐证）——"视觉笨拙"观感的主要来源之一；需按场景标定默认 preset 构图。
  - **P2-4 resume 恢复体验**：恢复到接近结尾时打开即"死画面"；建议 ≥90% 进度时回到 0 或给"从上次结尾继续"的明确提示。

**S87 用户体验复验（2026-08-28，项目负责人）**：✅ **通过**——重启 dev server 后在新 Electron 窗口完整体验，无问题。证实此前"完全崩溃"为旧 dev server HMR 半更新导致的运行态损坏（环境因素，非代码缺陷）。

**S87 收尾修复验收（2026-08-31，留言 24 走查，提交 e93832d）**：P0-1 云状态诚实性 ✅（实测假 key→诚实 offline、i18n 降级提示就位）；P1-2 默认 0.8x ✅（实测生效）；P2-4 resume 夹紧 ✅；**P1-3 C5 构图 ⚠️ 部分通过**——更低更近但 10m 释放点被裁出上沿，已退回工程再标定（建议 [7,5,9]/[0,5,0]/fov55-58 或 fit-to-height，留言 29）。新观察：内置自由落体场景 PhaseCard/公式条英文残留（S84 尾巴），随 P1-3 一并处理。P1-3 闭环后 S87 正式收官，进入 S88（2D 动画系统）切分评估。

**S87 最终闭环（2026-08-31，留言 25 走查，提交 0e7e3ff）**：✅ **S87 全部通过，第 18 轮收官**。P1-3 再标定至 [7,5,9]/[0,5,0]/fov56——释放点（t=0, y=10）、自由下落中（t=0.6s, y=8.24）、落地（t=1.45s, y=0.20）三时段实测全入画；英文残留清理（PhaseCard "释放/自由下落/落地"+ 副标题"重力作用下的匀加速运动"、公式条 `v=√(2·g·h₀)`、EventPulse"与地面接触"）通过。**第 18 轮结束，已开第 19 轮：请工程评估 S88（2D 动画系统）Sprint 切分，留言 30**。

**S85 真实题目实测阻塞（2026-08-31 复查）**：云配置 model=deepseek-chat ✅ 但 **apiKey 仍为空、baseUrl 被重置回 openai 默认值**——项目负责人重置 key 后尚未把新 key 与 `https://api.deepseek.com` 填入设置页。测试方案已交付（S85_CLOUD_AI_TEST_PLAN.md v1.0），填入后即可开测。

**S85 真实题目实测·中期报告（2026-09-02，留言 32，9 次真实云端生成）**：**未通过——2 个新 P0**。①**P0-A（红线级）规则兜底场景不加载**：云端失败→规则解析器成功产出场景（斜面/弹簧/碰撞等带 simulation 块）→ 应用停在首页、不切视图、不加载，仍是默认自由落体场景；自由落体题历来被"默认场景恰好也是自由落体"掩盖，从未暴露；②**P0-B 云端解析间歇性失败**（~5/9，同一题先败后成），失败原因被降级链吞掉；③P1-C 设置持久化脆弱（userData 在 %TEMP% + 强杀丢未刷盘写入——9/2 云配置再次丢失）。**修复验证通过项**：场景注入管线（h=45 参数保留、ohms_law 正确映射）✅、诚实失败守卫（刹车题明确拒绝）✅、max_tokens 4096（reasoning 1631-1797 余量充足）✅。测试方案：S85_CLOUD_AI_TEST_PLAN.md。

**S88-A 视觉规范已交付（2026-09-02，留言 33）**：`02_PRODUCT（产品设计）/2D_VECTOR_RENDERING_SPEC.md` v1.0——E1-E11 矢量元素体系（实体/轨迹/速度/力/加速度/辅助线/能量条/事件脉冲/四模板的完整渲染规格）、图解约定（矢量比例尺、出现消失动画、数值标注节奏、phase 连续性）、3D→2D 视角锁定视觉契约（S88-C 预留）、动效降级、自由落体纵切验收清单。与工程 scene2d-data.ts 投影层对齐（padding 8%/等比/y 翻转）。工程按此实现 SVG 渲染组件；冲突条目回设计侧调整。

**S85 最终报告（2026-09-02，留言 34，`S85_CLOUD_TEST_REPORT.md`）**：第一轮 17 次生成发现 2 个新 P0（P0-C 映射缺口错误动画 / P0-D 降级链覆盖云端正确拒绝——B1 写诗被硬凑成弹簧动画）。工程当日修复（299ccaa）。

**S85 复测闭环（2026-09-02 晚，留言 35）**：✅ **5/5 通过，S85 正式闭环**——T8 电路→ohms_law 正确加载、T9 波动→transverse_wave 正确加载、B1 写诗→诚实拒绝"这不是物理题"、B2/T10 类型正确（假设声明与简化提示为 P1 延后项）。**红线复测 0 错误动画。** 经验教训：HMR 半更新会制造假回归，验收必须全新进程。遗留 P1（不阻塞）：假设声明 / 简化提示 / 云端间歇性失败（~35-40%）/ AI 润色后台回归 / topic 枚举进 prompt。

**S88-B2 斜面走查（2026-09-02 深夜，留言 42，提交 82c8b06）**：路由就绪门控生效（斜面默认 2D ✓）、θ 标注/PhaseCard/**物理正确的加速度公式** ✓、G 矢量 ✓。**❌ P0 退回：速度矢量方向错误**（实测 45° 右下，应沿斜边左下，偏差约 108°——2D 图解教错物理）。小项：滑块未旋转贴合斜边、图形角度 26.6° vs 标注 30°。**平抛验收阻塞**：云端 key 再次丢失（根因=测试流程 taskkill /F 强杀致 localStorage 未刷盘，磁盘已无 key，需用户重粘；已建议工程 P1：设置改存主进程 settings.json）。规则解析器英文平抛题行为异常（先归 free_fall 后直接失败）待工程确认。欧姆定律 2D 已通过（留言 41）。

**Demo 三项验收（2026-09-03，留言 44，提交 8a0d0ba/5d7b56a/21d2ebe）**：✅ **打包产物验证通过**——`release\Physics Lab 0.1.0.exe`（95.79MB portable）双击启动成功（窗口正常）；✅ 斜面几何精确（tan30°=0.5774 分毫不差）、滑块 rotate(-30) 贴合、v 矢量象限修正（左下）+ |v| 长度物理精确；✅ settings.json 持久化机制走查通过。**❌ 最后一步退回：v 矢量方向 45° vs 应为 30°**（疑似 (-1,1)/√2 固定归一化而非 (-cosθ,sinθ)；修复公式已给出）。修完即斜面闭环、项目可开源发布。

## P2 第二步走查指引（给项目负责人）

1. 以"第一次使用的学生"心态操作：启动应用 → 输入或点选一道题 → 生成并看完一遍教学动画 → 自由探索（试着找参数调节、图表、解题步骤、知识图谱）
2. 过程中凭直觉操作，不要刻意记步骤；遇到"找不到某功能"的瞬间请记下来
3. 完成后：MenuBar →「导出使用数据」，把 JSON 发给设计 Agent
4. 评估指标（TEACHING_LAYER_SPEC §7.2）：核心路径 ≤3 次点击 / 四图标无零发现 / 无找不到功能的阻塞反馈
