# Current Milestone: S71 (Completed)

**Goal:** P0 题目输入首页 + P1 Teaching Layer 第一批模板（PhaseCard / FormulaStrip）
**依据:** TEACHING_LAYER_SPEC.md v1.0（产品/UX Agent 交付，第 1 轮问答确认）

**Completed:**
- [x] P0 题目输入首页：启动即首页（输入框自动聚焦 / 主 CTA 空输入禁用 /
      拍照识别（复用 OCR）/ 最近题目 5 条 / 示例题目 4 道 / 实验库折叠入口 /
      AI 在线状态弱提示）；状态机：空态 → 分步解析进度（实体→模型→编排动画）→
      失败（重试 / 示例场景兜底 FREE_FALL）→ 成功 300ms 淡入学习场景页
- [x] App 路由：HomePage ↔ AppShell（MenuBar 仅在学习场景页出现）
- [x] P1 Teaching Layer（2D HTML 覆盖层，pointer-events:none，确定性 state=f(t)）：
      PhaseCard 阶段卡（左上角、索引/标题/hint、进入 250ms 淡入 + 下移、
      离开 200ms 淡出、阶段开始自动减速 0.3x×1.2s）
      FormulaStrip 公式推导条（底部居中、三阶段：原式→代入→结果高亮、
      按 phase 进度 0/50/80% 切换、无数据 phase 自动隐藏）
- [x] PhysicsScene 追加可选 `overlay_hints` 字段（仅追加、向后兼容）
- [x] 统一显隐开关：MenuBar 视图菜单（教学图层总开关 + 阶段卡 + 公式推导条）
- [x] 低性能/减动效降级：prefers-reduced-motion 或总开关关闭 → 图层整体隐藏，
      退回既有侧面板教学
- [x] i18n：zh-CN / en-US 各 +26 键（home.*、teaching.overlay.*、menu.view.*，\uXXXX 规范）
- [x] 修复 MenuBar 视图菜单中遗留的字面 `n 文本 bug
- [x] 验证：shared tsc 0 错、desktop tsc 0 错、vitest 17 文件 124 用例全过、
      `npm run build`（Electron + renderer）成功

**待产品/UX Agent 验收（TEACHING_LAYER_SPEC §5）：**
- 首页仅一个主行动；实验库可达但折叠
- PhaseCard 阶段切换 + 动画减速可感知；拖动 Timeline 确定性回放
- FormulaStrip 三阶段同步；无数据 phase 自动隐藏
- 显隐开关 / 降级路径
- 五色语义一致且无颜色规则说明文字

**Deferred:**
- P1 剩余模板（ForceCallout、EventPulse）；P2 信息架构第一步（默认面板组合）
- PDF 题目输入；Ollama e2e 对比；变压器 + 简谐振动实验；en-US teacher.* 债务

**Next: S72** — P1 剩余模板（ForceCallout、EventPulse）+ P2 第一步（默认面板组合），
或产品/UX Agent 验收反馈修复
